package HashSet;

/*Pertenece a los set
 * elementos no duplicados, no puede haber repetidos
 * Codigo numerico
 * equals y hashcode
 * 
 * 1. HashSet: Almacena los elementos en una tabla hash: mejor rendimiento, no garantiza ningún orden.
 * 2. TreeSet. almacena los elementos ordenándolos en función
 * 3. Linkedhashset. Ordenando
 * 
 * compareto: criterio ordenación
 * equals + hascode: criterio de duplicidad
 */

/*Collections.sort
 * resverseOrder en orden descendente
 * sólo para listas
 * desordenar listass = collectiones.shuffle
 * para añadir en una posición menor = 
 * busqueda con binarysearch = 
 * collections.frequency = conteo de veces que aparece algo, devuelve numero entero
 * collections.min = saca el minimo del listado
 * collections.disjoint =  para ver todo lo que tienen en común, true = si no tienen nada en común; false = si
 * lista.of = es inmutable, no se puede ni agrandar ni quitar
 * LOS OBJETOS QUE SE COPIAN MANTIENEN LA MISMA DIRECCIÓN DE MEMORIA, SE VERÁN AFECTADOS EN EL OTRO ARRAY.
 */

/*listado3.addAll(listado1); listado 3 es todo lo que aparece en listado 1
 * listado3.retainAll(listado2); dime los elementos comunes con listado2
 */
