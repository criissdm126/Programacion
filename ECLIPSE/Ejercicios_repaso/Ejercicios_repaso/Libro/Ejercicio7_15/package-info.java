package Ejercicio7_15;
/*Diseña la clase Calendario que represneta una fecha concreta (año, mes y día).
* La clase debe disponer de los métodos:
*       Calendario (int año, int mes, int dia): que crea un objeto con los datos pasados como parámetros, siempre y cusndo, la fecha que represnta sea la correcta.
*       void incrementarDia(): incrementa en un día la fecha del calendario.
*       void incrementarMes(): incrementa en un mes la fecha del calendario.
*       void incrementarAño(int cantidad): incrementa la fecha del calendario en el número de años especificados. Ten en cuenta que el año 0 no existió.
*       void mostrar(). muestra la fecha por consola.
*       boolean iguales (Calendario otraFceha). que determina si la fecha invocante y la que se pasa como parámetro son iguales o distintas.
* Por simplicidad, solo tendremos en consideración que existen meses con distinto número de días, pero no tendremos en cuenta los años bisiestos.
*/
