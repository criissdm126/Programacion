package Examen;
/* Quiero que me cres una clase abstracta llamada Animal,
 * que tenga un método abstracto tostring, y que tiene 2 tipos que son gato y perro.
 * Esos tipos, tendrán un atributo llamado id ,nombre, color
 */

/* Luego créame una lista la cual recoja 10 objetos,los cuales sean 4 repetidos,
 * y luego creas otra lista que contenga la lista anterior, pero sin repetidos
 */

// Y la lista de los no repetidos, me la tienes que añadir en un mapa

// Y mostrarlo sin syso de mapa